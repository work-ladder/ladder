const { clearConsole } = require('@work-ladder/cli-shared-utils');

module.exports.clearConsole = async function clearConsoleAndTitle () {
  // const title = await exports.generateTitle(checkUpdate)
  clearConsole()
}
